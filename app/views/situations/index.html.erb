<div>
  <h2 class="text-3xl font-bold text-dark-blue">
    <i class="fa-solid fa-list"></i>
    履歴一覧
  </h2>
  <p class="mt-5 pl-5 text-calm-blue text-lg">過去に作成したやることリストを見直しましょう</p>

  <% @situations.each do |situation| %>
    <%= link_to situation_path(situation.id) do %>
      <div class="relative m-5 rounded-2xl border border-gray-200 bg-white p-5 text-dark-blue shadow-sm hover:-translate-y-1 hover:scale-[1.02] hover:opacity-80 sm:p-8">
        <p class="text-sm">
          <%= situation.created_at.strftime('%Y年%m月%d日 %H時%M分') %>
        </p>
        <hr class="mt-3 border border-gray-200">
        <p class="mt-2 block text-lg font-bold">
          <%= situation.fact %>
        </p>
      </div>
    <% end %>
  <% end %>

  <%= paginate @situations %>
</div>
